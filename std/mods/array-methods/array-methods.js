/*
(defmethod custom (array) (as an exercise) (in futility) )

(defunc
    (functions
        (bind)
        (apply)
        (call)
    )(arrays
        (map)
        (flat)
        (flatmap)
        (reduce)
    )
)

*/
