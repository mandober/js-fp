/**
 * NEEDLES   HAYSTACKS
 * bool      arr            true  in  [0, 1, true, false]
 *           set
 * str       str
 *           arr
 *           set
 *
 *
 * @param {*} needle
 * @param {*} haystack
 */
var subset = function (needle, haystack) {
    console.log(needle);
    console.log(haystack);

    // needle, haystack
};
